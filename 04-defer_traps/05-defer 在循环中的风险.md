# 05 - defer 在循环中的风险

## 1. 典型问题

在循环体里不断 `defer`，会等到函数退出才执行，导致资源累积。

```go
for _, p := range paths {
    f, _ := os.Open(p)
    defer f.Close() // 风险：可能一次打开很多文件
}
```

## 2. 正确做法

- 抽成小函数，让 defer 在每次迭代结束就执行
- 或在循环里显式 `Close()`

## 3. 面试一句话

> 循环 defer 容易造成句柄/连接堆积，是线上资源泄漏高频点。

