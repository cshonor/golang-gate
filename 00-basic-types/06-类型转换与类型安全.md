# Go 类型转换与类型安全

## 1. 强制规则

Go **没有隐式类型转换**，任何不同类型必须显式转。

```go
var a int = 10
var b int64 = 20
// a = b  // 编译错误

a = int(b) // 必须显式转换
```

## 2. 转换语法

```go
T(表达式)
```

## 3. 常见转换

- int ↔ int64
- float64 ↔ int
- string ↔ []byte
- string ↔ []rune

## 4. 危险点

- 大范围转小范围可能溢出
- float 转 int 会直接截断小数
