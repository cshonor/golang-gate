# 基础类型常见陷阱与最佳实践

## 1. 陷阱

1. int 长度不确定，跨平台用 int32/int64
2. string 按索引遍历是字节，不是字符
3. 不同数值类型不能直接比较/赋值
4. 浮点精度问题（不要直接 == 比较 float）
5. 混淆 rune 和 byte 导致中文乱码

## 2. 最佳实践

1. 业务默认用 int、float64、string
2. 网络/存储序列化用固定长度类型
3. 处理中文一定用 range 或 rune
4. 指针只在需要修改/避免拷贝时使用
5. 不要依赖隐式转换，Go 根本没有
