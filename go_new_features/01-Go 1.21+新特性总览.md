# 01 - Go 1.21+ 新特性总览

## 1. 你为什么要学“新特性”

你已经有：

- `datastruct/`：map/slice 的底层原理
- `error_handling/`：错误链与判断

新特性目录的目标是：补齐“**现代 Go 的日常写法**”，减少手写循环，提高可读性。

## 2. 这一组最常用的新能力

- `slices` 标准库：排序、查找、去重、插入删除等
- `maps` 标准库：拷贝、比较等
- `errors.Join`：把多错误合并成一个 error（常见于并发汇总）

> `iter` 属于更后续的新抽象（了解即可）。

