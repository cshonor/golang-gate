# Go TCP 客户端

> **04-tcp · TCP 协议与编程**

## 内容大纲

- `net.Dial` / `Dialer`：超时、KeepAlive、`DualStack`
- 连接池：通常由上层库实现；自研时注意 idle 超时与健康检查
- DNS 解析：`Resolver` 与连接复用
- 重试策略：幂等性、熔断与退避
- 调试：`net/http/httputil`、自定义 `Conn` 包装日志

## 正文

（待补充）
