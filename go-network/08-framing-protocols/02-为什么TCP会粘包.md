# 为什么 TCP 会粘包

> **08-framing-protocols · 粘包拆包与协议设计**

## 内容大纲

- Nagle、延迟 ACK、内核合并写（概念）
- 应用层 `write` 次数与 TCP 段边界无对应关系
- read 缓冲区任意切片：一次读到多个应用消息或半个
- 性能取向：减少 syscall 反而加剧「粘在一起」
- 结论：不是 bug，是语义；协议必须自描述长度或边界

## 正文

（待补充）
