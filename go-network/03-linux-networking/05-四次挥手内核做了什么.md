# 四次挥手：内核做了什么

> **03-linux-networking · Linux 网络底层**

## 内容大纲

- FIN 双向：`FIN_WAIT_1/2`、`CLOSE_WAIT`、`TIME_WAIT`、`LAST_ACK`
- 半关闭：`shutdown(SHUT_WR)` vs `close` 区别
- 2MSL 存在原因与 `TIME_WAIT` 堆积调优（预告 12 章）
- RST 场景：对端崩溃、端口未监听、向已关闭连接写
- 与 Go：`SetLinger`、优雅关闭实践

## 正文

（待补充）
