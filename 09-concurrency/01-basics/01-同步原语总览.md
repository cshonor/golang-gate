# 01 - 同步原语总览

## 1. 同步目标

1. **互斥**：`sync.Mutex` / `sync.RWMutex`
2. **等待一组任务结束**：`sync.WaitGroup`
3. **只初始化一次**：`sync.Once`
4. **条件等待/事件通知**：`sync.Cond` 或 channel

辅助：`sync.Pool`（复用）、`context`（取消/超时）。

## 2. channel vs 锁

- 共享状态：锁
- 编排与数据流：channel + select
