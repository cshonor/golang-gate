# Go TCP 服务端

> **04-tcp · TCP 协议与编程**

## 内容大纲

- `net.Listen` → `Accept` 循环；`TCPListener`/`TCPConn`
- 每连接 goroutine vs worker pool：取舍与背压
- `context` 取消、`SetDeadline`、优雅关停 `Shutdown`
- 错误处理：`Temporary` 已弃用后的写法；`errors.Is`
- 安全：限流、最大连接数、`TLS` 包装（扩展）

## 正文

（待补充）
