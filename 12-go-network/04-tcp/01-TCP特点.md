# TCP 特点

> **04-tcp · TCP 协议与编程**

## 内容大纲

- 面向连接、可靠、有序、字节流（无消息边界）
- 与 UDP 对比：延迟、头部开销、队头阻塞
- 适用场景：HTTP、RPC、数据库协议；不适合的场景（实时音视频可写扩展）
- Nagel 算法与延迟 ACK 对交互式应用的影响（预告）
- Go 默认行为：`SetNoDelay` 等常用调优入口

## 正文

（待补充）
