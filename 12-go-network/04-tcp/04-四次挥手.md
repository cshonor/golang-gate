# 四次挥手

> **04-tcp · TCP 协议与编程**

## 内容大纲

- 全双工关闭：每方向各发 FIN
- 为什么可能是三次：延迟 ACK 合并（概念）
- 主动关闭方进入 `TIME_WAIT` 的含义
- 优雅关闭流程：`shutdown` + 读侧读到 EOF
- 与 Go：`CloseRead`/`CloseWrite`（若版本/API 支持）与 `SetLinger`

## 正文

（待补充）
