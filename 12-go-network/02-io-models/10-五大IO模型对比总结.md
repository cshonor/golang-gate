# 五大 IO 模型对比总结

> **02-io-models · IO 模型全解**

## 内容大纲

- 一表汇总：延迟、吞吐、CPU、编程复杂度、平台支持
- 线程模型映射：per-thread blocking vs reactor vs proactor
- 典型中间件选型：nginx、redis、netty、Go net
- 陷阱清单：ET 漏读、`EPOLLOUT` 惊群（历史）、自旋锁化 event loop
- 自测：白板画出客户端发起到服务端 `read` 返回的完整路径

## 正文

（待补充）
