# 02 - defer 执行顺序规则

## 1. 顺序规则

多个 `defer` 按 **后进先出（LIFO）** 执行。

```go
defer fmt.Println("A")
defer fmt.Println("B")
// 输出: B -> A
```

## 2. 为什么重要

涉及多资源释放顺序时（锁、连接、临时文件）必须明确先后关系。

## 3. 实践建议

复杂场景把关键释放写注释，避免后续维护者误调顺序。

