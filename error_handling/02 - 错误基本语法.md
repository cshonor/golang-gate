# 02 - 错误基本语法

## 1. 标准返回签名

```go
func Read(path string) ([]byte, error)
```

## 2. 标准检查写法

```go
data, err := Read("a.txt")
if err != nil {
    return err
}
```

## 3. 创建错误

- `errors.New("...")`
- `fmt.Errorf("...")`

