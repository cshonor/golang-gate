# 04 - 错误包装与错误链

## 1. 为什么包装

底层错误要带上“在哪一步失败”的上下文，便于定位问题。

## 2. 标准写法

```go
if err != nil {
    return fmt.Errorf("query user id=%s: %w", id, err)
}
```

## 3. 错误链

`%w` 会保留原始错误，形成链路，后续可 `errors.Is/As` 分析。

