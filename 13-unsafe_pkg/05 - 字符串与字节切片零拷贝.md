# 05 - 字符串与字节切片零拷贝

## 1. 背景

- `string` 语义上只读
- `[]byte` 可写
- 普通转换会拷贝，零拷贝转换可减少分配

## 2. 风险

- 把字符串零拷贝成 `[]byte` 后写入，可能破坏不可变语义
- 容易引发难定位 bug

## 3. 建议

- 业务代码优先安全拷贝：`[]byte(s)` / `string(b)`
- 零拷贝只放在底层库，且严格限制“只读”

## 4. 面试一句话

> 零拷贝是性能优化，不是默认选择；先正确，再优化。

