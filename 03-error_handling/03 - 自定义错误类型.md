# 03 - 自定义错误类型

## 1. 为什么要自定义

当你需要携带字段信息（如 code、field、id）时，用结构体错误更合适。

## 2. 示例

```go
type BizError struct {
    Code int
    Msg  string
}
func (e *BizError) Error() string { return e.Msg }
```

## 3. 好处

- 上层可以按类型分流
- 日志更结构化

